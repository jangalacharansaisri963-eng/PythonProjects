#ifndef TOKENIZER_H
#define TOKENIZER_H

#include <string>
#include <vector>


using namespace std;


// Converts input expression into tokens

vector<string> tokenize(
    const string& expression
);


#endif
